import React from 'react';
import { render } from 'configs/test-utils';
import { Section } from './section';

describe('Section', () => {
  // TODO: write tests
  it('should render', () => {
    render(<Section />);
  });
});
